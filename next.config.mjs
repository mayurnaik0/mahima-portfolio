/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // Replace 'mahima-portfolio' with your exact GitHub repository name
  basePath: '/mahima-portfolio', 
};

export default nextConfig;
